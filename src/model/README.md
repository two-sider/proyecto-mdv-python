# Modulo `model`

Esta capa define las entidades del dominio.

## Archivo principal

- `task.py`: declara la clase `Task`, que representa una tarea dentro del sistema.

## Responsabilidad

El modelo no imprime nada ni sabe como se guardan los datos. Solo representa informacion valida para la aplicacion.
