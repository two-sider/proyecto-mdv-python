# Carpeta `tests`

Este espacio queda preparado para pruebas automaticas.

Todavia no se agregan casos de prueba, pero `requirements.txt` ya deja `pytest` como dependencia sugerida para extender el proyecto.
